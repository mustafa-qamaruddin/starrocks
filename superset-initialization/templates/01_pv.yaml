apiVersion: v1
kind: PersistentVolume
metadata:
  name: {{ .Values.pv.name }}
spec:
  accessModes:
    - ReadWriteMany
  capacity:
    storage: {{ .Values.pv.size }}
  hostPath:
    path: {{ .Values.pv.hostPath }}